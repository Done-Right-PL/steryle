# Hoisted linker keeps Metro (Expo) and AWS Amplify happy in a pnpm workspace:
# the isolated linker leaves symlinks that Metro's resolver and Amplify's
# Next.js build both drop.
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
enable-pre-post-scripts=true
