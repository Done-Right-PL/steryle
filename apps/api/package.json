{
  "name": "@steryle/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/local.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'api: skip'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.4",
    "@steryle/core": "workspace:*",
    "@steryle/db": "workspace:*",
    "hono": "^4.8.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.162",
    "@types/node": "^20",
    "tsx": "^4.20.3",
    "typescript": "^5"
  }
}
