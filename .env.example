# Copied to `.env` / `.env.production` for SST deploy (`pnpm deploy:backend`).
# SST injects these into the API Lambda.

# Razorpay — leave MOCK=true until live keys are ready.
RAZORPAY_MOCK=true
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
