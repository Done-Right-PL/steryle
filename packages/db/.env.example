# Local admin / seed against the deployed DynamoDB table (filled after `pnpm deploy:backend`).
# AWS_PROFILE=steryle-admin
# TABLE_NAME=

ADMIN_EMAIL=admin@steryle.in
ADMIN_PASSWORD=steryle-admin

# Razorpay (API / local API). Mock is on by default — set MOCK=false when keys are ready.
RAZORPAY_MOCK=true
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
